// Top-level build.gradle (Project level)
// Configures Gradle plugin versions for the entire project.

plugins {
    id 'com.android.application'        version '9.2.0' apply false
    id 'com.android.library'            version '9.2.0' apply false
}
